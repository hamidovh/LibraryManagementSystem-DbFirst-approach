@model LibraryManagementSystem.DAL.Rol

@{
    ViewBag.Title = "Sil";
}

<h2>Sil</h2>

<h3>Rolu silmək istədiyinizdən əminsiz?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RolAdi)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RolAdi)
        </dd>

    </dl>

    @using (Html.BeginForm("DeleteRol", "RolIdaresi", FormMethod.Post)) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Sil" class="btn btn-danger" /> 
            <div>@Html.ActionLink("Siyahıya Qayıt", "IndexRol")</div>
        </div>
    }
</div>
